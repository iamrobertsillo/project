---
import { getContentBySlug } from '../../utils/content-loader';
import Layout from '../../layouts/Layout.astro';
import HomeTemplate from '../../templates/HomeTemplate.astro';

const content = getContentBySlug('page', 'home', 'en');
---

<Layout title={content.title} lang="en">
  <HomeTemplate content={content} />
</Layout>